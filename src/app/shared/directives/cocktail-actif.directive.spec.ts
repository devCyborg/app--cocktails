import { CocktailActifDirective } from './cocktail-actif.directive';

describe('CocktailActifDirective', () => {
  it('should create an instance', () => {
    const directive = new CocktailActifDirective();
    expect(directive).toBeTruthy();
  });
});
